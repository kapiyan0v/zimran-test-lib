export {
  ABTestClient,
  createABTestClient,
  ABTestError,
} from './core';

export type {
  UserData,
  ExperimentConfig,
  ABTestClientOptions,
  ABTestPlugin,
  ConfigTransport,
  InitUserOptions,
  UpdateUserOptions,
} from './types';
